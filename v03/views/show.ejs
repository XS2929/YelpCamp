<%- include("partials/header") %>

<p><%= campground.name %></p>

<img src="<%= campground.image%>">

<p><%= campground.description %></p>

<% campground.comments.forEach(function(comment){ %>
	<p>
		<strong><%= comment.author %></strong> - <strong><%= comment.text %></strong>
	</p>
<% }); %>
<%-	include("partials/footer") %>